info:
  name: List Bills
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/bills"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
